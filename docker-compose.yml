services:
  labelcheck:
    build: .
    ports:
      - "8123:8123"
    mem_limit: 4g          # PaddleOCR + buffers; exit 137 during warmup = raise this
    environment:
      - PORT=8123
      - COLACLOUD_API_KEY=${COLACLOUD_API_KEY:-}   # read from .env (or shell) for registry pipelines
